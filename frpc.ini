[common]
# frp服务器地址（使用免费服务）
server_addr = frp1.chuantou.org
server_port = 7000

# 认证token（可选）
# token = your_token_here

[aki-excel]
type = http
local_ip = 127.0.0.1
local_port = 8080
# 使用子域名（更稳定）
subdomain = aki-excel

# 自定义域名配置（如果域名已解析到frp服务器）
# custom_domains = aki-excel.liuchenglu.com