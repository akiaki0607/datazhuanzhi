[common]
# 使用自建frp服务器（需要您有自己的服务器）
server_addr = your-server-ip.com
server_port = 7000

# 认证token（如果服务器需要）
# token = your_token_here

[aki-excel]
type = http
local_ip = 127.0.0.1
local_port = 8080
# 使用子域名
subdomain = aki-excel
# 或者使用自定义域名
# custom_domains = aki-excel.liuchenglu.com
